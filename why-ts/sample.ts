// ts 사용시 타입을 명시하면 명시한 다른 값을 넣을 경우 에러가 난다.
// 변수로 함수를 받았을 경우 해당 타입에 맞는 lntellisense 가 추천이 된다. (intellisense = 자동완성)
// 타입스크립트 장점은 에러의 사전방지 및 코드 가이드 , 자동완성이 있다
// function add(a: number, b: number): number {
//   return a + b;
// }

// var result2 = add(10, 20);

// return 이 number 타입
// result2.toLocaleString();
