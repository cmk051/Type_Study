// function sum(a, b) {
//   return a + b;
// }

// var result = sum(10, 20);

// @ts-check
// js 를 ts로 나타내기
/**
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */
// function sum(a, b) {
//   return a + b;
// }

// sum(10, 20);
// js파일은 ts와 다르게 type을 다르게 설정하면 에러표시가 나지 않는다.
// @ts-check를 사용하면 type 체크를 하게 된다.
// sum(10, "20");
